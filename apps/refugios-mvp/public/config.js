// Backend URL configuration
// - Local / Express serving static files: leave empty (uses relative URLs)
// - Render Static Site deployment: set to the backend service URL
//   e.g. "https://refugios-api.onrender.com"
window.__BACKEND_URL = "";
