---
phase: 02-campos-de-reserva
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - apps/refugios-mvp/public/index.html
  - apps/refugios-mvp/public/main.js
autonomous: false
requirements:
  - RES-03
  - RES-04
  - RES-05
  - RES-06

must_haves:
  truths:
    - "Owner can see a Limpieza number input in the reservation creation form"
    - "Owner can select Fuente as Booking.com / Airbnb / Web / Directo / Otro"
    - "Owner can select Temporada as Alta / Baja / Temporada / Permanente"
    - "Owner can select Tipo Documento as Boleta / Factura / Booking / Ninguno"
    - "Creating a reservation with all 4 fields filled saves and the values appear when viewing the reservation detail"
    - "Editing an existing reservation updates the 4 fields via PATCH (if edit form exists)"
    - "Source labels in the reservation list cards show Booking.com/Directo/Otro — not the old values"
  artifacts:
    - path: "apps/refugios-mvp/public/index.html"
      provides: "4 new form fields in reservation form"
      contains: "cleaning_supplement"
    - path: "apps/refugios-mvp/public/main.js"
      provides: "Updated sourceLabels, new seasonLabels, new reservationDocLabels, normalize() extension, chip rendering"
      contains: "seasonLabels"
  key_links:
    - from: "apps/refugios-mvp/public/main.js"
      to: "/api/reservations"
      via: "fetch POST/PATCH with new fields in payload"
      pattern: "cleaning_supplement|season_type|reservation_document_type"
    - from: "apps/refugios-mvp/public/main.js"
      to: "apps/refugios-mvp/public/index.html"
      via: "bindReservationForm reads named inputs from form"
      pattern: "bindReservationForm|toPayload"
---

<objective>
Add four new form fields to the reservation form in index.html and update main.js to include them in API payloads, render them as display chips in reservation cards, and update label maps.

Purpose: The database and API are ready after Plans 02 and 03. The UI is the final layer that makes the fields accessible to the owner. This plan closes Phase 2 with a human verification checkpoint.
Output: Updated index.html with 4 new form inputs/selects, updated main.js with new labels, normalize() extension, chip rendering, and verified end-to-end flow.
</objective>

<execution_context>
@/home/rreyes/.claude/get-shit-done/workflows/execute-plan.md
@/home/rreyes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-campos-de-reserva/02-RESEARCH.md
@.planning/phases/02-campos-de-reserva/02-03-SUMMARY.md

<interfaces>
<!-- Key patterns from codebase. Extracted from 02-RESEARCH.md code examples. -->

Current source select in index.html (lines ~762-770) — REPLACE entirely:
```html
<!-- OLD — remove these options: web, airbnb, phone, walkin -->
<select name="source" required>
  <option value="">Seleccionar</option>
  <option value="web">Web</option>
  <option value="airbnb">Airbnb</option>
  <option value="booking">Booking</option>
  <option value="direct">Directo</option>
  <option value="other">Otro</option>
</select>
<!-- NEW -->
<select name="source" required>
  <option value="">Seleccionar</option>
  <option value="booking">Booking.com</option>
  <option value="airbnb">Airbnb</option>
  <option value="web">Web</option>
  <option value="direct">Directo</option>
  <option value="other">Otro</option>
</select>
```

Form field pattern (from index.html reservation-form section):
```html
<div class="form-row">
  <label class="form-field">
    <span>Label text</span>
    <select name="field_name">
      <option value="">Seleccionar</option>
      <option value="val1">Display 1</option>
    </select>
  </label>
  <label class="form-field">
    <span>Numeric label</span>
    <input type="number" name="field_name" min="0" inputmode="numeric" />
  </label>
</div>
```

normalize() in main.js (lines ~321-327) — extend numeric keys array:
```javascript
for (const key of ["guest_id", "reservation_id", "sale_id", "guests_count", "cabin_id", "nights"]) {
  if (key in payload) payload[key] = parseInt(payload[key], 10) || 0;
}
for (const key of ["amount", "total_amount", "nightly_rate"]) {  // ADD: "cleaning_supplement"
  if (key in payload) payload[key] = parseFloat(payload[key]) || 0;
}
```

sourceLabels in main.js (lines ~12-19) — UPDATE:
```javascript
// OLD
const sourceLabels = { web: "Web", airbnb: "Airbnb", booking: "Booking", phone: "Teléfono", walkin: "Mostrador", other: "Otro" };
// NEW
const sourceLabels = { booking: "Booking.com", airbnb: "Airbnb", web: "Web", direct: "Directo", other: "Otro" };
```

New label maps to add near existing sourceLabels:
```javascript
const seasonLabels = { alta: "Alta", baja: "Baja", temporada: "Temporada", permanente: "Permanente" };
const reservationDocLabels = { boleta: "Boleta", factura: "Factura", booking: "Booking", ninguno: "Ninguno" };
```

Chip rendering in renderList callback (main.js ~line 1228):
```javascript
${row.cleaning_supplement ? chip(`Limpieza $${money.format(row.cleaning_supplement)}`) : ""}
${row.season_type ? chip(seasonLabels[row.season_type] || row.season_type) : ""}
${row.reservation_document_type ? chip(reservationDocLabels[row.reservation_document_type] || row.reservation_document_type) : ""}
```
Note: The `money.format()` or `CLP` formatting used for `total_amount` should be reused for `cleaning_supplement`. Find the actual formatter used in the file.

IMPORTANT: main.js is 2849 lines. Make targeted, surgical edits. Do NOT rewrite the file. Each change site is a small, well-defined location. Read the file first to confirm line numbers before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 form fields to index.html and update source select</name>
  <files>apps/refugios-mvp/public/index.html</files>
  <action>
Read `apps/refugios-mvp/public/index.html` to locate the reservation form section. Find the existing `<select name="source">` element.

**Change 1 — Replace source select:**
Find the `<select name="source">` element and replace its options with the new vocabulary:
```html
<select name="source" required>
  <option value="">Seleccionar</option>
  <option value="booking">Booking.com</option>
  <option value="direct">Directo</option>
  <option value="other">Otro</option>
</select>
```
Keep the `required` attribute. Only the options change.

**Change 2 — Add 3 new form fields:**
Find a logical position in the reservation form to add the new fields. Recommended: add them in a new `<div class="form-row">` after the existing source/payment_method row.

Add the following fields:
1. `cleaning_supplement` — number input:
```html
<label class="form-field">
  <span>Suplemento Limpieza (CLP)</span>
  <input type="number" name="cleaning_supplement" min="0" step="500" inputmode="numeric" placeholder="0" />
</label>
```

2. `season_type` — select:
```html
<label class="form-field">
  <span>Temporada</span>
  <select name="season_type">
    <option value="">Seleccionar</option>
    <option value="alta">Alta</option>
    <option value="baja">Baja</option>
    <option value="temporada">Temporada</option>
    <option value="permanente">Permanente</option>
  </select>
</label>
```

3. `reservation_document_type` — select:
```html
<label class="form-field">
  <span>Tipo Documento</span>
  <select name="reservation_document_type">
    <option value="">Seleccionar</option>
    <option value="boleta">Boleta</option>
    <option value="factura">Factura</option>
    <option value="booking">Booking</option>
    <option value="ninguno">Ninguno</option>
  </select>
</label>
```

Group them in two `form-row` divs for visual balance:
- Row A: cleaning_supplement + season_type
- Row B: reservation_document_type (alone or paired with another field)

Note: These 3 selects/inputs are NOT `required` — they are optional fields (nullable in DB). Do not add `required` attribute.

After editing, verify the HTML is valid by checking the form structure manually or running the app.
  </action>
  <verify>
    <automated>grep -n "cleaning_supplement\|season_type\|reservation_document_type" /home/rreyes/projects/cvti/clients/cabanas-mvp/apps/refugios-mvp/public/index.html | head -20</automated>
  </verify>
  <done>
index.html contains `name="cleaning_supplement"`, `name="season_type"`, `name="reservation_document_type"` inputs.
Source select now has exactly 3 options: booking, direct, other.
No HTML syntax errors (file is valid HTML).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update main.js — labels, normalize(), and chip rendering</name>
  <files>apps/refugios-mvp/public/main.js</files>
  <action>
Read `apps/refugios-mvp/public/main.js` to confirm line numbers for each change site before editing. The file is 2849 lines — use targeted edits only.

**Change 1 — Update sourceLabels (near top of file, ~line 12-19):**
Find `const sourceLabels` and replace its content with the new vocabulary:
```javascript
const sourceLabels = {
  booking: "Booking.com",
  direct: "Directo",
  other: "Otro"
};
```

**Change 2 — Add new label maps after sourceLabels:**
Immediately after the updated `sourceLabels` declaration, add:
```javascript
const seasonLabels = {
  alta: "Alta",
  baja: "Baja",
  temporada: "Temporada",
  permanente: "Permanente"
};
const reservationDocLabels = {
  boleta: "Boleta",
  factura: "Factura",
  booking: "Booking",
  ninguno: "Ninguno"
};
```
Define these at file scope (same level as `sourceLabels`), NOT inside a function. See RESEARCH.md Pitfall 5.

**Change 3 — Extend normalize() numeric cast for cleaning_supplement (~line 325):**
Find the line:
```javascript
for (const key of ["amount", "total_amount", "nightly_rate"]) {
```
Add `"cleaning_supplement"` to the array:
```javascript
for (const key of ["amount", "total_amount", "nightly_rate", "cleaning_supplement"]) {
```

**Change 4 — Add chip rendering in reservation list render callback (~line 1228):**
Find the `renderList("reservations-list", ...)` call and its callback template literal. Find where existing chips are rendered (e.g., `chip(\`Canal ${sourceLabels[row.source]}\`)`). Add three new chip lines after the existing chips:
```javascript
${row.cleaning_supplement ? chip(`Limpieza ${formatMoney(row.cleaning_supplement)}`) : ""}
${row.season_type ? chip(seasonLabels[row.season_type] || row.season_type) : ""}
${row.reservation_document_type ? chip(reservationDocLabels[row.reservation_document_type] || row.reservation_document_type) : ""}
```
Find the actual money/currency formatter used in the file (search for how `total_amount` is formatted in chips — it may be `Intl.NumberFormat`, a `money` object, or a `formatMoney()` function). Use the same formatter for `cleaning_supplement`.

**Change 5 — Update bindReservationForm / form population for edit flow:**
If there is an edit/populate form function that sets initial values from an existing reservation (search for function that sets form field values from a reservation object), add population for the 4 new fields. Patterns to find:
- `form.elements['source'].value = row.source` or similar
- Set `cleaning_supplement`, `season_type`, `reservation_document_type` values when populating the form for edit

If no such function exists (form is create-only), skip this step and note it in the summary.

After all changes, verify JavaScript syntax by running:
```bash
node --check apps/refugios-mvp/public/main.js
```
  </action>
  <verify>
    <automated>node --check /home/rreyes/projects/cvti/clients/cabanas-mvp/apps/refugios-mvp/public/main.js && echo "syntax OK"</automated>
  </verify>
  <done>
main.js passes `node --check` (no syntax errors).
sourceLabels contains booking/direct/other keys only.
seasonLabels and reservationDocLabels exist at file scope.
normalize() includes cleaning_supplement in the float cast array.
Chip rendering includes cleaning_supplement, season_type, reservation_document_type.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — 4 new fields end-to-end</name>
  <action>No automated action — this checkpoint verifies the complete end-to-end flow in the running application. Tasks 1 and 2 have already made all code changes.</action>
  <verify>
    <automated>MISSING — visual/functional verification requires human interaction with the browser UI</automated>
  </verify>
  <done>Owner confirms all 4 fields are visible, saveable, and displayed correctly in reservation cards and detail view.</done>
  <what-built>
    Four new reservation fields added end-to-end:
    - Limpieza supplement (number input) — RES-03
    - Source/Fuente select with Booking.com/Directo/Otro — RES-04
    - Temporada select with Alta/Baja/Temporada/Permanente — RES-05
    - Tipo Documento select with Boleta/Factura/Booking/Ninguno — RES-06
    PATCH /api/reservations/:id endpoint for full reservation editing.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd apps/refugios-mvp && npm start` (or check existing running instance)
    2. Log in at https://refugios.onrender.com (or localhost if running locally)
    3. Open the reservation creation form
    4. Verify all 4 new fields are visible: Limpieza input, Fuente select (3 options), Temporada select (4 options), Tipo Documento select (4 options)
    5. Verify the Fuente select shows only: Booking.com, Directo, Otro (no Web/Airbnb/Teléfono/Mostrador)
    6. Create a test reservation with: Limpieza=10000, Fuente=Directo, Temporada=Alta, Tipo Documento=Boleta
    7. Verify the new reservation appears in the list with chips showing the values
    8. Open the reservation detail — verify all 4 new fields are visible with correct values
    9. (If edit form exists) Edit the reservation, change Temporada to Baja — verify it saves via PATCH
    10. Existing reservations with old source values (web/airbnb etc.) should now display as Booking.com/Directo/Otro
  </how-to-verify>
  <resume-signal>Type "aprobado" if all 4 fields work end-to-end, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `node --check apps/refugios-mvp/public/main.js` exits 0 (no syntax errors)
2. `grep -c "cleaning_supplement\|season_type\|reservation_document_type" apps/refugios-mvp/public/index.html` returns >= 3
3. `grep -c "seasonLabels\|reservationDocLabels" apps/refugios-mvp/public/main.js` returns >= 4
4. Human checkpoint: owner can create reservation with all 4 fields and see them in list/detail view
</verification>

<success_criteria>
- All 4 form fields visible and functional in the reservation creation form
- Source select updated to 3 options (Booking.com/Directo/Otro)
- Chip rendering shows new fields in reservation list cards
- main.js has no syntax errors
- Human verification approved: owner can create, view, and edit reservations with the 4 new fields
- Phase 2 success criteria met: RES-03, RES-04, RES-05, RES-06 all demonstrably working
</success_criteria>

<output>
After completion, create `.planning/phases/02-campos-de-reserva/02-04-SUMMARY.md`
</output>
