---
phase: 06-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/refugios-mvp/public/main.js
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "La vista Ventas abre por defecto mostrando el mes actual, no el año completo"
    - "La vista Gastos abre por defecto filtrada al mes actual, no vacía"
    - "El admin puede cambiar el período manualmente sin fricción — los inputs existentes siguen funcionando"
  artifacts:
    - path: "apps/refugios-mvp/public/main.js"
      provides: "Ambas correcciones de valor inicial: periodType.value y expensesFilterMonth"
      contains: 'periodType.value = "month"'
  key_links:
    - from: "state.expensesFilterMonth"
      to: "expenses-filter-month input element"
      via: "bindExpensesFilters() — month.value set before addEventListener"
      pattern: 'month\.value = state\.expensesFilterMonth'
    - from: "periodType.value"
      to: "updatePeriodVisibility()"
      via: "valor 'month' dispara visibilidad del selector de mes"
      pattern: 'periodType\.value = "month"'
---

<objective>
Fix the default period for the Ventas and Gastos views so both open showing the current month instead of requiring manual filter selection every session.

Purpose: Reduces friction for the admin — zero extra clicks to see the current period on both views.
Output: Two targeted edits to main.js, no new files.
</objective>

<execution_context>
@/home/rreyes/.claude/get-shit-done/workflows/execute-plan.md
@/home/rreyes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-polish/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ventas — cambiar período por defecto de "year" a "month" (UX-01)</name>
  <files>apps/refugios-mvp/public/main.js</files>
  <action>
    En main.js línea 3875, cambiar:
      if (periodType) periodType.value = "year";
    por:
      if (periodType) periodType.value = "month";

    Eso es todo. El selector de mes (monthSelect) ya tiene el mes actual pre-seleccionado en la línea 3872 (`monthSelect.value = String(currentMonth).padStart(2, "0")`), así que al cambiar el tipo a "month", la vista de Ventas abrirá directamente en el mes actual.

    No tocar ninguna otra línea de esta función. No agregar localStorage ni ninguna persistencia (deferred en CONTEXT.md).
  </action>
  <verify>
    <automated>grep -n 'periodType.value' apps/refugios-mvp/public/main.js</automated>
  </verify>
  <done>La única asignación de periodType.value en el archivo es `"month"`, no `"year"`.</done>
</task>

<task type="auto">
  <name>Task 2: Gastos — inicializar filtro de mes al mes actual (UX-02)</name>
  <files>apps/refugios-mvp/public/main.js</files>
  <action>
    Dos cambios en main.js:

    1. Línea 113 — cambiar el valor inicial del estado:
       Antes: expensesFilterMonth: "",
       Después: expensesFilterMonth: new Date().toISOString().slice(0, 7),

    2. Dentro de `bindExpensesFilters()` (función que empieza en línea 3372), después de la línea:
         const month = document.getElementById("expenses-filter-month");
       y ANTES del bloque `if (month) { month.addEventListener(...)`, agregar:
         if (month) month.value = state.expensesFilterMonth;

    Esto sincroniza el DOM con el estado inicial para que el input `<input type="month">` muestre el mes actual cuando se monta la vista.

    No cambiar el event listener existente — ya hace `state.expensesFilterMonth = month.value` en cada cambio manual.
  </action>
  <verify>
    <automated>grep -n 'expensesFilterMonth' apps/refugios-mvp/public/main.js</automated>
  </verify>
  <done>
    Tres ocurrencias de expensesFilterMonth:
    1. Línea ~113: valor inicial con `new Date().toISOString().slice(0, 7)`
    2. Línea ~3374: `if (month) month.value = state.expensesFilterMonth;` (nueva)
    3. Línea ~3382: event listener existente sin modificar
  </done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Verify Ventas fix
grep -n 'periodType.value' apps/refugios-mvp/public/main.js
# Expected: single result with "month", no "year"

# Verify Gastos fix — three occurrences expected
grep -n 'expensesFilterMonth' apps/refugios-mvp/public/main.js
# Expected: line ~113 (state init with toISOString), line ~3374 (DOM sync), line ~3382 (event listener)

# Syntax check
node --check apps/refugios-mvp/public/main.js
# Expected: no output (clean parse)
```
</verification>

<success_criteria>
1. `node --check apps/refugios-mvp/public/main.js` exits 0 — no syntax errors introduced
2. `grep 'periodType.value = "year"' apps/refugios-mvp/public/main.js` returns empty — old default gone
3. `grep 'expensesFilterMonth: new Date' apps/refugios-mvp/public/main.js` returns a match — Gastos state initialized to current month
4. `grep 'month.value = state.expensesFilterMonth' apps/refugios-mvp/public/main.js` returns a match — DOM synced on bind
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish/06-01-SUMMARY.md` with:
- What was changed (exact lines and before/after values)
- Verification commands run and their output
- Commit hash
- Any observations about adjacent code that might be relevant for future phases
</output>
