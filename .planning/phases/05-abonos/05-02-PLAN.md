---
phase: 05-abonos
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - apps/refugios-mvp/public/main.js
autonomous: false
requirements:
  - PAY-01
  - PAY-02
  - PAY-03

must_haves:
  truths:
    - "No 'Migrar pago' button is visible anywhere in the reservation cards UI"
    - "The 'Abonar' button appears for reservations that are pending or partial"
    - "Reservations #2191, #2196, #2197 show as 'pending' (not 'paid') after deploy"
    - "A newly created reservation with no initial_payment shows as 'pending'"
  artifacts:
    - path: "apps/refugios-mvp/public/main.js"
      provides: "UI without the Migrar pago button"
      note: "The function definition can remain — only the button rendering call is removed"
  key_links:
    - from: "Reservation card HTML (line ~2288)"
      to: "renderReservations function"
      via: "template literal"
      pattern: "onclick=\"migrateReservationPayment"
---

<objective>
Remove the "Migrar pago" button from the reservation card UI and verify the complete fix is working in production after deploy.

The button `migrateReservationPayment` was accidentally triggered by the client on 3 recent 2026 reservations, creating fake full-payment abonos. The endpoint stays for potential future admin use, but the button must not be visible to the client.

Purpose: Prevent accidental re-triggering of the migration button on real reservations. Combined with Plan 05-01 (backend fixes + data cleanup), this closes all three bugs in the abonos system.

Output: Clean main.js (no Migrar pago button) deployed to Render, human verification that the 3 affected reservations now show correct debt status.
</objective>

<execution_context>
@/home/rreyes/.claude/get-shit-done/workflows/execute-plan.md
@/home/rreyes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-abonos/05-01-SUMMARY.md
@apps/refugios-mvp/public/main.js

<interfaces>
<!-- Relevant section of main.js around line 2287-2288 (the button to remove) -->
<!-- Line 2287: Abonar button (KEEP) -->
${row.debt_status !== "paid" ? `<button ... onclick="openSaleModalForReservation(...)">Abonar</button>` : ""}
<!-- Line 2288: Migrar pago button (REMOVE THIS ENTIRE LINE) -->
${row.paid_amount == 0 && Number(row.total_amount) > 0 ? `<button ... onclick="migrateReservationPayment(...)">Migrar pago</button>` : ""}

<!-- The migrateReservationPayment() function definition at ~line 4566 can stay.
     Only the button rendering line (~2288) must be deleted. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Migrar pago button from reservation card template</name>
  <files>apps/refugios-mvp/public/main.js</files>
  <action>
    Delete the single line in main.js that renders the "Migrar pago" button inside the reservation card template literal. This is around line 2288.

    The line to remove looks like:
    ```javascript
    ${row.paid_amount == 0 && Number(row.total_amount) > 0 ? `<button type="button" class="btn btn--sm btn--ghost" onclick="migrateReservationPayment(${row.id}, '${(row.guest_name || "").replace(/'/g, "\\'")}') " title="Marcar como pagada (reserva antigua)">Migrar pago</button>` : ""}
    ```

    Remove ONLY this line. Do not:
    - Remove the `migrateReservationPayment` function definition (lines ~4566-4592) — the endpoint stays for potential admin use
    - Change the "Abonar" button on line 2287
    - Change any other logic around debt_status or paid_amount display

    After removal, the record-actions div should have: Abonar button (conditional), Huésped button, Cabaña button, Editar button, and (for cancelled) Delete button.
  </action>
  <verify>
    <automated>grep -n "onclick=\"migrateReservationPayment" apps/refugios-mvp/public/main.js</automated>
  </verify>
  <done>
    The grep for `onclick="migrateReservationPayment` returns zero results — the button rendering call is gone. The migrateReservationPayment function itself (~line 4566) remains intact and still appears in `grep -n "migrateReservationPayment" apps/refugios-mvp/public/main.js`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all three fixes in production</name>
  <action>
    Push current branch (fix/billing-total-and-duplicate-records) to trigger Render deploy. Wait for deploy to complete (check Render dashboard or use tools/push-and-wait-render.sh if available). Then follow the verification steps below.
  </action>
  <verify>
    <automated>git push origin fix/billing-total-and-duplicate-records</automated>
  </verify>
  <done>Human confirms all 7 checks pass (see how-to-verify below).</done>
  <what-built>
    Three fixes deployed together:
    1. debt_status SQL: reservations with total_amount = 0 now return 'pending' (not 'paid')
    2. Migration 038: deleted the 3 incorrect migration abonos (sale ids 1970, 2025, 2037)
    3. UI: removed "Migrar pago" button from all reservation cards

    Deploy branch: fix/billing-total-and-duplicate-records — Render auto-deploys on push
  </what-built>
  <how-to-verify>
    After deploy completes, visit https://refugios.onrender.com and:

    1. Navigate to Reservas tab
    2. Find reservation #2191 (Eric Bartel) — should now show a "Debe $X" chip (pending or partial), NOT "No debe"
    3. Find reservation #2196 (Ana Paula Vera) — should show pending with full $2,660,000 balance
    4. Find reservation #2197 (Eric Schlor) — should show pending with full $435,000 balance
    5. Confirm NO "Migrar pago" button is visible on any reservation card
    6. Find any reservation with total_amount = 0 — confirm it does NOT show a "pagado" chip
    7. Click "Abonado $0 ▾" on any of the 3 affected reservations — confirm payment history is empty
  </how-to-verify>
  <resume-signal>Type "aprobado" if all 7 checks pass, or describe which check failed and what you see instead</resume-signal>
</task>

</tasks>

<verification>
- `grep -n "onclick=\"migrateReservationPayment" apps/refugios-mvp/public/main.js` returns no results
- `grep -c "migrateReservationPayment" apps/refugios-mvp/public/main.js` returns a small number (function definition only, ~4566-4592)
- After prod deploy: reservations #2191, #2196, #2197 show non-paid debt status
- No "Migrar pago" button visible in the UI
</verification>

<success_criteria>
- "Migrar pago" button removed from reservation card template in main.js
- Human confirmed that the 3 affected 2026 reservations now show correct (non-paid) debt status in production
- Human confirmed no "Migrar pago" button is visible in the UI
</success_criteria>

<output>
After completion, create `.planning/phases/05-abonos/05-02-SUMMARY.md` with:
- The line number removed from main.js
- Human verification outcome (what the client confirmed)
- Confirmation of which reservation IDs are now showing correct status
</output>
